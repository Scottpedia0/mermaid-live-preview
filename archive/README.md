# Archived variants

These folders are preserved as historical implementation variants and are **not** the active source tree.

## Archived folders

- `extension/` — original baseline with in-webview source editing/apply flow
- `cloud-pass-2/` — improved block targeting, but still built around the older edit/apply model

## Active folder

- `../cloud-pass-3/` — preview-first, Markdown-only workflow and the canonical implementation path for current work
